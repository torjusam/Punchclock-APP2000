/**
 * @file for setting up an alternative datepickers which can support four individual datepickers
 * @module Extra
 * @author Magnus A, Ask I.P.A, Torjus A.M
 */
/*import DatePicker from "react-datepicker";
import {addDays, addHours} from "date-fns";
import React, {FC} from "react";
import Employee from "../../../../utils/employee";
import "react-datepicker/dist/react-datepicker.css";

interface pickersProps {
    employee?: Employee;
    start: Date;
    end: Date;
}

const DateAndTimePickers: FC<pickersProps> = ({start, end}) => {

    return (
        <div>
            <h2> Nåværende stempling </h2>
            <DatePicker

    );
};*/